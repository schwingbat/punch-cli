// Generate an invoice in plain text.

module.exports = function(config, data, outPath) {
  return new Promise(resolve => {
    console.log("Plaintext invoice not implemented yet");
    resolve();
  });
};
