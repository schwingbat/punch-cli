{{> ui/nav-tabs selected="clients" }}

<div class="clients-index page">
  <div class="container">
    <section>
      <header>
        <h1>Clients</h1>
      </header>

      <ul class="clients-index__list unstyled">
        {{#each clients}}
          <li>{{> client-link . }}</li>
        {{/each}}
      </ul>
    </section>
  </div>
</div>
